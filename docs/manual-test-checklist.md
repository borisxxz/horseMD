# 功能清单 + 手动测试清单(重构回归基线)

> 重构的安全网。每条 = 一个可手动复现的步骤 → 预期。重构每抽一块,跑相关
> 条目;全部重构完后做整表回归。勾选框 `[ ]` 供手动执行时打勾。
> 平台:macOS arm64(`/Applications/HorseMD.app`)为主,Windows/Linux/移动在分节标注。
>
> 自动化执行约束：普通 CDP 回归必须在隐藏的后台测试窗口中运行，不获取
> 系统焦点；输入规则、换行、光标、模式切换和源码保真用例必须逐字符输入。
> 粘贴、IME composition 和真实窗口层级行为另设专项，不得用批量
> `Input.insertText` 假装真人键入。

---

## A. 标签页 / 窗口
- [ ] 双击文件树多个 md → 各开一个标签,只有一个窗口
- [ ] 切换标签 → 编辑器跟随,已挂载的编辑器不重建(秒切)
- [ ] 关闭有未保存内容的标签 → 弹确认;关闭无内容的 → 直接关
- [ ] 关闭最后一个文件 → 进 Home,大纲清空(#20)
- [ ] 拖拽标签?/ 右键「在分屏打开」→ 右格出现,两格都可编辑
- [ ] macOS 关窗口(红绿灯)→ 进程保留;Cmd+Q 有未保存 → 弹确认
- [ ] Windows:最小/最大/关闭按钮工作;最大化图标状态同步
- [ ] Linux:GTK 风格最小/最大/还原/关闭按钮工作;拖动顶栏、双击最大化正常;关闭脏文档弹确认

## B. 文件树 / workspace
- [ ] 打开文件夹 → 侧边栏树展开;增删改文件实时刷新(watcher)
- [ ] **桌面外部拖入打开**：从 Finder / 文件资源管理器把单个与多个 Markdown 拖到窗口正文、顶栏和空白区，松开后各打开一次且最后一个成为当前标签；拖入文件夹后加入多根工作区并自动显示文件树；同一批文件 + 文件夹可同时处理。拖放过程中显示提示层，松开或取消后立即消失。把图片拖到富文本正文仍插入当前文档，不得打开图片标签；标签、侧边栏和大纲的内部拖拽不触发提示层。自动化：`npm run test:drop-open-ui`
- [ ] 外部改了已打开文件 → 编辑器提示重载(文件监听回显)
- [ ] 右键文件:重命名 / 删除 / 在 Finder 显示 / “导出”子菜单；子菜单包含 PDF、HTML、Word、EPUB、LaTeX、ODT、RTF、TXT，悬停、点击及键盘 `→`/`↑`/`↓`/Enter 均可操作
- [ ] 深层目录可展开/折叠;文件多的大文件夹不卡死(性能项)
- [ ] **单一工作区 + 多根**:侧边栏顶部显示「工作区」;「打开文件夹」选另一个文件夹 → 作为第二棵树加入(原文件夹不消失);右键根节点可「从工作区移除」
- [ ] **根节点保护**:根节点可展开/收起/新建子项,但不能拖拽移动、不能重命名/删除真实文件夹;普通子文件/子文件夹仍可拖拽移动、重命名、删除
- [ ] 关窗重开 → 工作区里的多个文件夹根都恢复
- [ ] **文档位置记忆（#111）**：打开一个长文档（富文本或重文档均可），把光标移到文档中部并滚动到对应位置，关闭 app 重新打开：视口应落在上次的位置附近，不需要重新滚动；把光标停在正文某段后重启，点击正文后输入，文字应落在重启前的位置附近。用外部编辑器修改文件后再打开（长度变化）：应从顶部打开、不套用旧位置。自动化：`npm run test:doc-position-restore-ui`
- [ ] 旧 session(单 workspace)打开后自动迁移成一个工作区,不丢
- [ ] **飞书式斜杠菜单**:输入 `/` 弹全量;`/h1`/`/H1`/`/标题1`/`/#`→标题1;`/有序`/`/ol`/`/1.`→有序列表;`/无序`/`/ul`/`/-`→无序列表;`/图片`/`/image`/`/img`→图片;`/代码`/`/code`→代码块;`/表格`/`/table`→表格。打字收窄不消失;无匹配时 Enter 正常换行;Esc/退格关闭。特别用复杂文档测试 `/code`：创建后不做中间保存，依次编辑代码、代码块后正文和前文列表，再切源码、保存、冷重开；三处必须一致且不得出现保存暂停。自动化：`npm run test:tail-fence-ui`

## B.1 云同步（桌面）
- [ ] 设置 → 云同步：添加一个已有文件夹，仅创建根目录 `.horsemd/workspace.json`；文件不移动，侧边栏和 watcher 不显示 `.horsemd`
- [ ] 添加 HTTPS WebDAV / S3 连接：错误地址或错误凭据失败；成功后连接列表不显示密码或 Secret；“编辑”可修改公开配置，密码或 Secret 留空仍可通过测试并保留原值；可选 User-Agent 留空正常，通过后只在服务商明确要求时填写且不能输入换行
- [ ] 无冲突/删除时点击“双向同步”图标只需一次点击；存在冲突或删除时在当前文件夹下方展开确认预览，且“查看预览”不执行传输
- [ ] 同步预览不使用全屏弹窗；文件清单限制高度并可滚动，关闭图标只收起当前文件夹的预览
- [ ] 云端连接和已连接文件夹的操作为带 tooltip 的图标按钮；同步中显示旋转图标，成功后显示勾选图标
- [ ] “上传本地到云端”：远端同名旧文件和远端额外文件先进入远端 `.horsemd/trash/`，本地文件不被改动
- [ ] “从云端下载到本地”：本地同名旧文件和本地额外文件先移入本地 `.horsemd/trash/`
- [ ] 手工删除远端 `.horsemd/manifest.json` 或清空远端工作区后，“双向同步”显示恢复选择且不生成本地删除计划；选择“上传本地到云端”后远端恢复
- [ ] 空远端首次同步：预览的上传数量和文件数一致；确认后 Markdown、图片、附件均可在第二个隔离 profile 下载
- [ ] 第二个 profile：添加空本地文件夹 → 加入已有云端工作区 → 先看到下载预览 → 确认后内容正确，不产生重复工作区 ID
- [ ] **发布前真实 WebDAV 验收**：使用独立的第三方 HTTPS WebDAV 测试空间（推荐 Koofr），测试连接后上传一个含中文、空格和括号文件名的 Markdown、图片及子目录；第二个隔离 profile 加入同一工作区并下载，逐项核对内容、文件名和附件。测试空间必须与日常/正式同步空间隔离。
- [ ] 两端修改同一个文件：预览显示冲突；同步后原文件和“冲突副本”都存在，任一版本没有静默丢失
- [ ] 一端删除、另一端未修改：预览显示删除；本地和远端 `.horsemd/trash/` 中保留可恢复副本
- [ ] 断网、错误凭据、远端文件在预览后改动：同步停止且显示错误，不推进本地同步状态；重新预览后可恢复

## C. 命令面板 / 快捷键
- [ ] ⌘P → 命令面板,可搜文件 + 命令
- [ ] 选中文字后 ⌘B/Ctrl+B → 加粗且侧边栏不变化;⌘⇧B/Ctrl+Shift+B → 切侧栏;⌘F → 查找;⌘⌥F → 替换(聚焦替换框)
- [ ] 设置 → 编辑器 → 编辑 → 关闭“选中文字时显示浮动工具栏” → 选区不再弹出浮动栏；选中文字后右键，一级菜单保持紧凑的“文字格式”“审阅标记”“转换为”，悬停或键盘聚焦后展开子菜单；格式、审阅新增/删除/替换/高亮 + 评论，以及列表或块类型转换均可用，点击任一选区操作只作用于原选区；靠近窗口右侧时子菜单向左展开且不被裁切；重新开启后下一次选区正常显示浮动栏
- [ ] ⌘S → 保存;⌘/ → 源码/富文本切换;⌘P 命令齐全(find/replace/theme/source/outline/files)
- [ ] 设置 → 键盘快捷键：搜索 `save`/`保存` 能找到保存命令；显示的默认键位与菜单/命令面板 hint 一致
- [ ] 录制 `保存` 为 ⌘⌥S/Ctrl+Alt+S → 设置页、命令面板 hint 和菜单 accelerator 同步；按新快捷键只保存一次
- [ ] 尝试把 `保存` 录成 ⌘O/Ctrl+O → 显示冲突命令，不写入配置；尝试录成单字母 `A`、Enter、Tab → 显示保留键原因，不写入配置
- [ ] 清空 `切换侧边栏` → ⌘⇧B/Ctrl+Shift+B 不再响应；单项恢复默认后重新响应；全部恢复默认后所有自定义项清空
- [ ] 设置页激活时按 ⌘⇧B/⌘F 或对应 Ctrl 组合 → 不打开侧边栏/查找栏；离开设置页后同一快捷键恢复生效
- [ ] 重启应用 → 自定义快捷键仍存在；清空或恢复默认后的状态也保持
- [ ] 大纲默认保留前两层实际层级：H1→H2→H3 只收起 H3；全 H1 文档不隐藏任何标题
- [ ] 源码/富文本往返后大纲标题、层级和折叠状态不跳变；代码块内的 `#` 不进入大纲
- [ ] 模式切换在前/中/后至少 10 次：可见光标上下文一致且仍可见，纯滚动阅读时视口顶部内容一致；另以 400KB+ 长文档在富文本滚到中后部且**不点击正文**后切源码，源码不得回到顶部，切回富文本也不得跳。至少在一段含 `$…$` 行内公式的正文中点击光标并连续执行富文本→源码→富文本→源码，源码和富文本都必须仍是该公式后的同一段文字
- [ ] 连续链分别覆盖 `源码→富文本→源码→富文本` 和 `富文本→源码→富文本→源码`；表格、列表、图片后方及 fenced code block 内的光标均可逆且可见
- [ ] fenced code block 内分别点击前/中/后行再切到源码，光标落在对应代码行而非统一落到围栏开头；表格单元格内的反引号代码也保持字符位置
- [ ] 源码粗光标为 3px 且略高于文字行；在 100k+ 自动换行文档前/中/后点击时贴合当前行，不遮住无关文字、不出现在空白视口

## D. 源码 ↔ 富文本
- [ ] transaction→source 新路径仍处于分类放行阶段。修改任何事务同步代码时，必须先跑 `npm run test:source-transaction-sync-ui` 证明显式 primary 路径，再跑本节全部家族项证明生产旧路径没有回归；专项通过不能替代完整段落/空块/列表/反引号验证
- [ ] .md 默认富文本;.txt 默认纯文本(textarea)
- [ ] 富文本末段已有文字时，点击正文下方空白再输入 → 原末段不变，输入出现在新段落；末尾已有空段落时重复点击不产生额外空行
- [ ] ⌘/ 切源码 → 显示 markdown 源码;再切回 → 富文本,内容不丢
- [ ] 使用含连续标题、单个 `~`、紧凑 `-` 列表和列表项末尾 `\\` 的 Markdown：不编辑直接富文本↔源码往返后逐字符一致；在富文本末尾只输入一个字符后，源码仅新增该字符，不新增空行、不转义原有 `~`、不把 `-` 改为 `*`
- [ ] 在已有文档、真正空白新文件中执行“两次 Enter → 逐个输入 8 个空格（每次停顿）→ 逐字输入 abc”，立即切源码：视觉上只显示 8 个空格与 abc，不得出现 `&#x20;`，不得拼到上一段或在行尾制造空格；源码 raw 应为不可见 `U+200B` + 8 个 ASCII spaces（Typora 同款）。富文本↔源码连续往返两次，光标仍在 c 后；保存并彻底退出重开后磁盘逐字节不变。另覆盖嵌套列表/列表续行。自动化：`npm run test:leading-space-entity-ui` + `npm run test:markdown-preservation`
- [ ] 在上述紧凑列表任一项目中修改一个字并按 Enter 新增一项，保存、关闭并重开：原列表仍使用 `-`，项目之间没有新增空行，标题/段落之间的原始空行逐字符不变
- [ ] 使用含 `第一行\n第二行` 的正文，只修改第二行一个字后切源码：仍只有原来的单换行，不得自动插入空行；在文档末尾正文按 Enter 输入新段落后切源码：两个正文之间必须恰有一个空白行。保存、彻底退出并重开后，新正文仍是独立段落
- [ ] 打开包含 `第一行\n第二行\n第三行` 的已有文档：默认富文本按三行显示；切源码仍只有两个普通单换行，富文本↔源码往返和保存都不得补空行、两个尾随空格或 `<br>`。显式硬换行仍分行且源码写法不变
- [ ] “设置 → 编辑器 → 编辑”关闭“保留源码单换行”：上述三行在富文本按 CommonMark 合并显示；重新开启后立即恢复分行。两种状态下磁盘源码均逐字符不变，Enter 仍创建段落，Shift+Enter 仍创建硬换行
- [ ] 在富文本连续输入第一段、按 Enter 输入第二段、再按 Enter 输入第三段，输入最后一个字后不等待，立即点击底部源码按钮或按 Ctrl/Cmd+/：三段内容必须立刻完整出现，段落间各有一个空白行；不能暂时丢字、合并到同一行，也不能依赖切回富文本才恢复
- [ ] 新建空文档后分别测试两条路径：在默认 H1 手打标题，停顿约 1 秒后按 Enter，再以相同节奏写两段正文；跳过 H1 直接在正文写两段。最后一个字输入后不保存并立即切源码：前者包含真实 `# 标题`，后者不能凭空出现空 `#`，正文不能并入标题或相邻段落，源码不能出现 Crepe 空段落占位 `<br />`；保存并全新进程重开后结构不变
- [ ] 在“前段正文”和后续标题/正文之间分别测试：按 Enter 后立刻输入、按 Enter 停顿 1 秒再输入、连续按两次 Enter 再输入。每条都执行富文本→源码→富文本→源码：新文字保持独立段落，源码不出现 `<br />`，后续块不移动
- [ ] 打开含多个段落的文档，把其中一个段落文字全部删除（或用退格把空段落里的字符删光，例如输入 `.` 删除、输入 `/` 删除）后切源码：该段落只能变成空行，源码绝不允许出现 `<br />`，相邻段落与空行原样保留；往返多次后空行不累积。自动化：`npm run test:empty-paragraph-source-ui`
- [ ] 打开源码含 `前段。* **输入设备：** 内容` 这类「行中 `* ` 使源码与 canonical 可见流分叉」的文档，选中 `输入设备：` 并删除，**不保存**立即切源码：删除的文字必须消失，源码保留作者字面 `* `（不得变成 `\*` 或 `&#x20;`）；保存并全新进程重开后，删除内容不得重新出现。自动化：`npm run test:diverged-delete-source-ui`
- [ ] 在引用块内清空一行后切源码：只能变成空的 `>` 行，不能出现 `> <br />`；回到富文本再按一次 Backspace 删除空引用块，切源码时 `>` 也必须消失，保存并完整重开不能复活。两个连续空引用删除其中一个时，源码必须从两个 `>` 精确变成一个。两个相邻 `-` 列表之间的空行删除或仅往返切换时，两个列表的 `-` 都保持原样、不得变成 `*` 或黏成一行；新建文档输入正文/列表后立即切源码，末尾只允许一个换行，不得多出空行。自动化：`npm run test:source-fidelity-probes`、`npm run test:paragraph-source-ui`、`npm run test:empty-blockquote-removal-ui`
- [ ] **跨块连续编辑事务**：打开同时含标题、普通列表、`- 4. 文本`、加粗和后文列表的已有文件；快速执行“标题末尾新增文字 → 删除中间列表项文字 → 修改后文列表项”，不等待界面稳定立即切源码。第一次源码必须同时包含两处新增并移除旧文字；再往返、保存、完全退出重开后结果不变，未编辑空行、引用和 marker 逐字符保留。自动化：`npm run test:mixed-rich-source-transaction-ui`
- [ ] **列表项内字面标记**：分别在有序和无序列表项正文逐字输入 `1. 测试`、`1) 测试`、`- 测试`、`+ 测试`、`* 测试`，最后一个字符后立即切源码；只能得到 `外层 marker + 原始字面文本`，不得增加 `\`。同一文件中未编辑的 `-`、`+`、`*` 和紧凑空行必须保持；作者原来手写的 `1\.`、`\-` 后续编辑时不得被删除。保存并完整重开后再次核对。自动化：`npm run test:list-item-literal-marker-source-ui`
- [ ] **反引号删除与强制边界**：逐字输入一个反引号并删除，立即切源码；再逐字输入三个反引号，先删两个并切源码（必须剩一个原始反引号），再删最后一个并立即保存。重复两轮并继续输入普通正文；不得出现“保存已暂停”、源码按钮无响应、标题/段落粘连或旧字符复活。再用含两条相同反引号行、独立空段落、Setext 标题和未编辑列表的文件重复，保存并完整进程重开。自动化：`npm run test:code-fence-delete-source-ui`
- [ ] **复杂分叉文档直接保存**：打开同时含 `- -` 嵌套解析、字面三反引号、空引用和多处重复“测试”的既有文档；分别在独立“测试”正文、`- - 测试 1. 你好` 的嵌套项、以及它后面的兄弟项末尾逐字输入内容，每次都不切源码直接保存。三处都不得出现“保存已暂停”；随后切源码、完整退出并重开，源码和磁盘必须只增加本次文字，两个作者短横线及其他列表、引用、空行和反引号写法逐字节不变。自动化：`npm run test:diverged-ordinary-save-ui`
- [ ] **重复引用长会话**：在上述复杂文件里连续修改同一引用中的三段文字，再定位到第三个相同的 `> 测试`，按两次 Enter 退出引用并逐字输入一个唯一末段；不先切源码，直接保存。首次源码、磁盘与完整重开必须都保留末段，且末段绝不能被拼进前面任何同名引用。自动化：`npm run test:diverged-ordinary-save-ui`
- [ ] **引用后空白直接起笔**：打开末尾是引用、下方仍可点击空白正文的复杂分叉文件，不在引用内按 Enter，直接用鼠标点击引用下方空白处并逐字输入唯一文字；立即保存后切源码并完整重开。文字必须位于文档末尾普通正文，不能消失，也不能变成前面任意位置的 `>文字`。自动化：`npm run test:diverged-ordinary-save-ui`
- [ ] **保存边界 settle 与恢复副本**：在列表输入规则、反引号删除、引用退出和连续跨块编辑的最后一个按键后立即按保存/切源码，不能因延迟 `markdownUpdated` 瞬时弹出保存暂停。构造持续不可映射事务时，原文件必须逐字节不变，保存入口应弹出另存恢复副本；取消不写任何文件，确认只写 `<原名>.horsemd-recovered.md`，不能清除原标签未保存状态。自动化：`npm run test:editor-flush-settle`、`npm run test:source-sync-recovery`
- [ ] 在空正文中分别逐字输入 `- 第一项`、`* 第一项`、`+ 第一项`，按 Enter 添加第二项，再连续往返富文本/源码并保存重开：三种 marker 必须各自保持，列表项之间不增加空行；已有紧凑列表末尾新增项也必须继承原 marker，不能复制或吞并相邻任务/有序列表
- [ ] 在已有正文末尾逐字输入附加文字，按 Enter，逐字输入 `-`、空格、`列表第一项`；切源码后必须是独立 `- 列表第一项`，不得并入上一段、变为 `*` 或出现 `<br />`。保存、关闭并重开后再次核对。
- [ ] 新建未保存文档：默认 H1 输入标题，正文输入一行，再**连续不停顿**地逐字输入 `1.`、空格、第一项；Enter 输入第二项；再 Enter、Tab 输入嵌套项（列表段不要每步停顿 0.5s，那样掩盖问题）。连续执行富文本→源码→富文本→源码：必须保留 `1. 第一项`、`2. 第二项`、缩进 `1. 嵌套项` 三层源码，不能合成一行、丢失外层或只剩内层；嵌套项与外层项之间不得多出空行（必须紧凑，与富文本显示一致）。自动化：`npm run test:rich-source-continuous-fidelity-ui`
- [ ] 新建未保存文档，H1 输入标题，再逐字输入 **3 个外层有序项**（`1.` 空格 文字 Enter，共 3 项），在末项按 Tab 缩进输入 **2 个嵌套项**。**分别用「连续不停顿」和「较慢（每键 ~120ms）」两种节奏各测一次**（慢节奏更容易触发 bug），连续执行富文本→源码→富文本→源码：必须得到 `1./2./3.` 外层 + 缩进 `1./2.` 嵌套的完整源码，**绝不能出现 `1. 文字   2.文字` 这种把嵌套项压到同一行的合并**。自动化：`npm run test:rich-source-nested-3x2-fidelity-ui`
- [ ] **RS-46 行内 nested ordered 拆项**：临时文件写 `- 1. 甲乙\n- 丙丁`，在富文本 `甲|乙` 中间按 Enter，再逐字输入使第二项变成 `新乙`。切源码必须得到同一 outer bullet 内可重解析的 `- 1. 甲\n  2. 新乙\n- 丙丁`（具体 continuation 缩进跟随 outer marker content column），不得变成 `- 2. 新乙` 新外层 bullet；保存、冷重开后仍是一父 bullet + 内层 ordered 1/2 + `丙丁`。自动化：`npm run test:nested-number-list-source-ui`
- [ ] 用临时文件输入 `- 1. 管理层`、`- 2. 综合行政部`、`- 3. 人力资源部`，在第二项正文开头连续按 Backspace：先删除字面 `2. `，再把列表项逐级提升。每一步切源码都必须保留“综合行政部”正文；最终只能改变该行的 marker/缩进，不得删除正文、写坏前面的标题或让保存重开后旧结构复活。自动化：`npm run test:diverged-list-structure-ui`
- [ ] **RS-82 非空 bullet 段并入左侧 ordered list**：准备 `2. 左项`、空行、两个非空 `-` 项，后面再放一个必须保持独立的 `1. 后续父项` 及其 nested child。在第一个 bullet 正文开头按一次 Backspace；富文本应把两个 bullet 变为同一 ordered list 的连续 `3.`/`4.`，后续父项仍是另一棵 ordered list且 child 仍嵌套。源码必须只改这三个语法 marker：两个 moved marker 与用于分隔列表的 following `1)`；不得改正文、nested child、fence、空行、CRLF 或其它 marker。直接保存并完整冷重开后结构与源码不变，全程不得出现“不一致/保存暂停”提示。自动化：`npm run test:nonempty-bullet-backspace-merge-ordered-ui`
- [ ] **RS-83 中间独立 thematic break 输入规则**：在一个全局已有 source/canonical marker 分叉的文档中，准备有序列表 `1./2./3.`，其后还有另一棵必须保持独立的有序列表与 U+200B/混合 marker 嵌套列表。给第三项输入正文，连续按两次 Enter 退出列表；在中间空段逐字输入第一个 `-`，等待它以独立 `\\-` 安全发布，再快速输入第二、第三个 `-` 触发分隔线。富文本必须出现独立 `<hr>`，源码必须是 `3. 正文\n\n---\n\n1. 后续项`，不得变成 `3. 正文***`、遗留 `\\-`、合并两棵列表或改写其它 marker。保存并完整冷重开后分隔线、两棵列表和源码字节保持一致，全程零“不一致/保存暂停”提示。自动化：`npm run test:middle-thematic-break-input-rule-ui`
- [ ] **RS-84 跨列表反向选区连续 Backspace**：在全局已有 marker/U+200B 分叉的文档中依次准备 `- first`、独立 `2. middle`、`- last / - survivor`。用鼠标或 Shift 反向选中从 first 正文开头到 last 正文末尾，按一次 Backspace；富文本必须只留下位于 survivor 前的一个空 bullet，源码必须精确变为 `左段\n\n- \n- survivor`。立即再按一次 Backspace，空 bullet 应提升为空段，源码必须精确为 `左段\n\n- survivor`，不能残留 first/middle/last、不能吞掉 paragraph→list 的空行，也不能改 survivor marker。两拍均不得出现不一致提示；随后切源码、保存、完全退出并冷重开，源码和磁盘逐字一致。自动化：`npm run test:cross-list-selection-delete-empty-bullet-ui`
- [ ] **RS-85 空 ordered 父项仍带 nested child 的 Backspace**：从空文档依次输入 `1. 父项一`、Enter 输入 `父项二`、Enter 后 Tab 缩进输入 nested ordered child `子项`。用方向键回到父项二末尾，逐字 Backspace 清空正文但保留空 `2.` marker 和 nested child，再按一次 Backspace。富文本应只剩一个顶层 ordered item，第一项内顺序为“父项一正文、一个编辑器空段、nested ordered child”；源码必须精确只删除 `2. ` 行，成为 `1. 父项一\n   1. 子项`，不得写入 `<br />`、丢 child、提升 child 为顶层或弹出不一致提示。切源码、保存、完全退出并冷重开后结构和字节保持一致。自动化：`npm run test:empty-ordered-parent-before-nested-backspace-ui`
- [ ] **RS-86 mixed-marker bullet 中间项快速双 Enter**：在经过多轮列表创建、填充与保存后仍保留作者 `-` marker 的长文档中，准备同一 bullet 区域 `- u高科技`、`- 12312`、`- 1\\. 色粉色分`。将光标放在中间项 `12312` 末尾，快速连续按两次 Enter，不能等待第一拍 source callback 完成。富文本应把空 bullet 提升为位于 `12312` 与后继项之间的一个顶层空段，并把列表拆成前后两棵；`1. 色粉色分` 必须继续存在。源码应保持三条 authored bullet 逐字不变，不能删除后继项、写入 `<br />`、改正文或弹出“不一致/保存暂停”提示。随后切源码、直接保存并完全冷重开，三项与源码字节仍一致。真实 mixed-marker 三态由 `npm run test:markdown-preservation` 与 `npm run test:source-fidelity-probes` 覆盖；普通 all-`*` 物理按键不抢占控制组：`npm run test:rapid-double-enter-bullet-exit-before-sibling-ui`。
- [ ] 在上述数字点列表后另建含 `**ISO 9001:2015**` 的普通列表，逐字编辑其正文；再连续编辑三个相邻且 marker 分别为 `-`、`+`、`*` 的列表（填空项、加一项、删一项），不等待回调就切源码并保存重开。35/55/65ms 与 burst 节奏都要覆盖；所有编辑必须同时存在，三个 marker、紧凑间距以及删除列表后到下一块的空行数量保持作者原样。自动化：`npm run test:rich-source-chaos-ui`
- [ ] 用 CRLF 临时文件写 `+ 1. AB` 与后续普通列表；编辑数字点正文、把一个项目拆成普通缩进续行、一次连续插入两个同级项，并分别把第一项/唯一项完全提升成正文。切源码、保存和新进程重开后，CRLF、`+` marker、续行缩进、X/Y 顺序与剩余列表必须完全一致；禁止落入通用映射后重排文字。纯函数覆盖：`npm run test:markdown-preservation`
- [ ] 同一回调的对抗检查：前一个列表新增一项、后一个列表删除一项且总行数保持不变；数字点分歧列表改正文的同时改变后续标题层级。前者必须保留两个列表各自 marker/边界，后者若不能完整映射必须原子回滚且不得推进 canonical baseline，绝不能只保存列表编辑却静默丢掉标题结构。自动化：`npm run test:markdown-preservation`、`npm run test:source-fidelity-probes`
- [ ] **多轮保存/重开后继续编辑**：用含重复“测试”、`- 1. 文本`、混合行尾和既有有序列表的文件，先修改列表项并保存重开；第二轮删除刚加文字、继续有序列表、退出后手打 `- 无序项` 和 fenced code，再保存重开；第三轮继续该无序列表、Enter 退出并输入正文，再保存重开；第四轮在这段正文与后续 fence 之间 Enter 新段，逐字输入正文、`1. ` 有序列表两项、退出列表和尾文，再保存并第五次冷打开。每轮首次源码、第二次无编辑往返、磁盘和冷重开必须完全一致，不得少空行、把 `-` 改为 `*`、丢列表/尾文、产生 lone `\r` 或只提交半个事务。自动化：`npm run test:family-multicycle-ui`；真实文件可用 `FILE=/absolute/file.md node scripts/test-family-multicycle-ui.mjs`（脚本只操作临时副本）。
- [ ] **P0：RS-41 安装包真实长会话**：不能用上一个自动化条目代替。先核验运行进程来自
  `/Applications/HorseMD.app` 且版本为当前候选；**启动该真实手测进程时必须把
  `--horsemd-input-trace` 传入 Electron argv**（例如安装包可用 `open -a /Applications/HorseMD.app --args --horsemd-input-trace ...`）。
  不得只开 `ELECTRON_ENABLE_LOGGING` 就让用户开始复现，因为它抓不到首个源码同步事务的
  `source / candidate / canonical`。确认 trace 已启用后，再打开真实复杂长文档的临时副本；在文章
  末尾用 `/code` 新建代码块，逐字输入代码，退出后输入正文，再返回附近或前文列表/正文
  做新增、删除和改写。连续至少 10 轮，交替执行“保存→源码→富文本”和
  “源码→富文本→保存→完整退出重开”。每轮逐项比较富文本可见内容、源码 textarea、
  磁盘字节和冷重开结构；保存不报错但内容不同仍算失败。0.13.47 已在此项人工失败，
  在用户明确验收前不得发布稳定版或回复 issue 已解决。事故记录：
  `docs/rich-source-divergence-incident-0.13.47.md`。
- [ ] 切到源码后立即用鼠标移动光标并输入，等待 1 秒再检查：光标和输入内容不能被延迟恢复任务拉回先前的富文本位置；再切回富文本和源码，字符位置保持不变
- [ ] 在源码的硬换行第二行末尾放置光标，切回富文本后不等待，立即 Enter 并依次输入 `X`、停 140ms、`Y`、停 140ms、`Z`；等待 1 秒后必须得到独立 `XYZ` 段落，上一行不增加 `YZ`。再切源码应为 `上一行\n\nXYZ`，光标位于 `Z` 后；连续执行 10 次
- [ ] **图片替代文字（#84）**：含 `![测试图片](image/test.png)`、带标题的 `![替代说明](image/test.png "图片标题")` 和旧版尺寸图片的文档，在富文本编辑并连续保存后，普通图片说明不被改为 `1.00`，标题和旧版尺寸图片仍正确
- [ ] 在富文本中全选后粘贴上述原始 Markdown → 保持富文本结构；切源码后仍逐字符保留粘贴内容，不新增空行、不把 `0~9` 改为 `0\\~9`、不把 `-` 改为 `*`
- [ ] 从微信公众号或网页粘贴含标题、加粗、图片的富文本，即使纯文本回退含 `1. ...` 等 Markdown-like 内容，也保留 HTML 的标题层级、行内格式和图片
- [ ] 源码模式编辑 → 保存 → 内容正确(非受控 textarea 同步)
- [ ] **源码 + 预览（桌面）**：在富文本编辑区右键并选择“源码 + 预览”后，左侧为源码、右侧为同一文件富文本；确认状态栏没有额外入口，且两侧正文都填满各自面板、只保留正常边距；左侧逐字修改标题、列表和表格，停顿后右侧更新；右侧仅可滚动、选中和复制，不能输入、不能显示块柄/浮动工具栏/应用右键菜单，也不能产生未保存标记。仅打开、关闭、滚动或拖动分隔线不得产生未保存标记。分别在首行和中段非空行的第一个字符前点击，尤其覆盖 `## 页面对应关系` 的首个 `#`，源码粗光标必须完整位于字符边界前，输入不得落到首字后；两侧滚到底时，左侧不得保留比右侧更多的尾部空白。右上角“关闭预览”必须直接回到普通富文本。左→右、右→左交替滚动 10 次，在普通段落、表格、图片和代码块附近都不得回顶或来回抖动；即使点击右侧预览，Ctrl/Cmd+F 仍应作用于左侧源码。自动化：`npm run test:source-rich-split`
- [ ] 用十六进制或脚本生成 UTF-8 BOM + CRLF 文档：只切换源码/富文本后保存状态不变；在富文本和源码各输入一个字符再保存，除这两个字符外磁盘字节完全一致，BOM 和全部 CRLF 保留
- [ ] 使用混合 LF/CRLF 文档，在源码模式执行普通输入、查找替换、添加审阅标记和插入附件：未触碰行保留各自换行风格，光标切回富文本仍落在同一字符
- [ ] 使用 Setext 标题、引用定义/引用链接、HTML、实体、自动链接、硬换行、紧凑列表、无外侧管线表格、代码和公式组成的文档，在前/中/后至少 12 处逐一编辑；每次切源码确认只有目标 raw 区间改变，最后保存并逐字节比较
- [ ] A 标签切源码,B 标签仍富文本;切回 A 仍源码(#42)
- [ ] 源码模式编辑后切到 B 标签再切回 A → textarea 内容仍在;切回富文本/保存都正确

## E. 大纲
- [ ] 侧栏切「大纲」→ 列出 H1–H6
- [ ] **悬浮章节导航（桌面）**：关闭侧栏大纲后，含标题文档右侧只显示少量圆点；悬停或键盘 Tab 聚焦后展开标题列表。鼠标点击某一章节后直接移出面板，列表必须立即收起，不需要点击正文；长标题不撑开编辑区，省略后仍能通过悬停看到完整标题
- [ ] **悬浮章节导航联动**：富文本和源码各自滚动/点击任一项 → 当前章节同步高亮并平滑跳转；超过 8 个标题时圆点保持紧凑且当前章节不会从圆点中丢失。打开侧栏“大纲”后右侧悬浮导航仍可用；分屏时仅服务最后聚焦的窗格
- [ ] 大纲顶部总开关：只有 H1→H2 的文档也可点击“全部折叠”，隐藏二级标题；再次点击“全部展开”恢复。H1→H2→H3 文档的首次默认仍保留 H1/H2，不受此开关影响
- [ ] 滚动文档 → 大纲高亮跟随当前标题(reflow-free scrollspy),不卡、停得准
- [ ] 大纲面板软居中:active 项漂到上下边沿 → 自动滚到中间;滚到文档底 → 最后标题也到中间
- [ ] 点大纲项 → 文档跳到该标题
- [ ] **大纲重排（桌面 #82）**：拖同一父级下标题左侧抓手到另一同级标题前/后 → 标题、全部子标题及正文整体移动；切源码确认无关空行、列表符和转义未被重写，再切回富文本确认层级顺序一致。尝试拖到另一父标题下或不同级标题上 → 不允许落下，避免隐式改父子关系
- [ ] 两篇标题不同的文档分屏 → 点击左右编辑区时大纲立即切为对应文档；点大纲只滚动当前焦点栏；左栏源码/右栏富文本时仍正确(#66)
- [ ] 关闭文件 → 大纲清空(#20)

## F. 主题 / 自定义主题
- [ ] 切 light/dark + 6 套内置主题(含莫兰迪)
- [ ] **跟随系统主题（#95）**：设置 → 外观开启“跟随系统外观” → 日间选一套浅色主题、夜间选一套深色主题；切换系统浅/深色偏好后正文、应用外壳、菜单和代码块即时跟随对应配对。重启后配对与开关仍保留；手动点任一主题后应退出跟随系统。自定义 CSS 片段仍生效，含 `@media (prefers-color-scheme: dark)` 的规则只在夜间生效。
- [ ] 放 Typora 风格 `.css` 到 userData/themes → 被识别、可应用、相对 url() 生效
- [ ] 切自定义主题后 app chrome 仍是自己的样式,写作区用主题

## G. 国际化
- [ ] 切中文/英文 → 所有 UI 文本、命令面板、斜杠菜单、查找替换、保存提示都跟随

## H. 查找替换(#19)
- [ ] ⌘F → 查找栏(带替换行,默认展开)
- [ ] 输入查找词 → 实时高亮 + `n/m` 计数;▲▼ 上一个/下一个(循环)
- [ ] 输入替换词 →「替换」替当前并跳下一个;「全部」替换全部
- [ ] 替换框 Enter=替一个,Shift+Enter=全部;Esc=关
- [ ] **富文本模式**:替换正确(块内、多匹配)
- [ ] **源码模式(⌘/)**:替换正确(非受控 textarea 适配)← 重点回归
- [ ] **源码模式查找跳转**:当前命中居中且有清晰高亮;连续上下一个均同步更新
- [ ] 查找栏保持打开时富文本↔源码双向切换 → 当前序号、定位和高亮保持
- [ ] 无查找词时替换按钮 disabled
- [ ] **匹配开关(0.13.48)**:`Aa` 区分大小写、`ab` 全字匹配(cat 不命中 catfish,中文按文字字符处理)、`.*` 正则(与全字互斥,切换自动互斥)计数各自正确;`Alt+C/W/R/L` 快捷键与按钮等效
- [ ] **正则细节**:非法正则(如 `[`)→ 输入框红边、计数归零、不崩溃;`^`/`$` 按行解释;替换框 `$1`/`$&`/`$$` 模板生效(替换全部后可一次撤销语义仍正确)
- [ ] **多行查询**:查找框 `Ctrl/Cmd+Enter` 换行或粘贴多行 → 匹配相邻行;富文本跨段落需 `\n`;替换框同样支持多行
- [ ] **在选区内查找**:选中一段文字 → 开启选区开关(多行选区 ⌘F 时自动开启)→ 计数只含选区内匹配;替换全部只改选区;关闭开关回到全文;切换模式/标签后选区范围自动失效回全文
- [ ] **IME 组合**:中文输入法在查找框内组词按 Enter 上屏 → 不得触发"下一个"
- [ ] 自动化:`node scripts/test-find-modes.mjs`、`node scripts/test-find-modes-ui.mjs`(需先 build)

## I. 保存 / 保存提示
- [ ] ⌘S / 浮动保存按钮 → 写盘,桌面端弹「已保存 ✓」(~1.5s 自隐)
- [ ] **富文本未保存即时提示（0.12.63）**：打开已保存文件，在正文、列表和表格各逐字输入一个字符；标签灰点、底部“已修改”和保存入口应在 180ms 内出现，但磁盘文件在未按 ⌘S 前不得变化。输入一个字符后立刻删除回保存内容，约 260ms 后灰点必须自动消失。自动化：`npm run test:rich-dirty-indicator-ui`
- [ ] **富文本立即保存（#105/#106）**：打开含两条图片链接的文件，在富文本正文逐字输入后不等待直接按 ⌘S；连续重复三次，每次切源码都能看到刚输入文字，两个图片链接各恰好一条。关闭并重开后正文不回退、图片不丢失也不重复
- [ ] **本地 Markdown 链接**：在已保存文档中分别写入 `[绝对路径](/tmp/target.md)`、`[相对路径](./target.md)` 与网页链接；在富文本 Cmd/Ctrl+点击前两者应交给系统打开对应本地文件且应用不导航离开，网页链接仍由默认浏览器打开。Windows 额外覆盖 `C:/...` 与 UNC 路径。自动化：`npm run test:local-markdown-links`
- [ ] **富文本删除不复活**：选中普通正文的一整段文字并删除，立刻保存后切源码、关闭文件、以全新窗口重开；该文字在富文本、源码和磁盘均不得重新出现。此项与“立即保存”一起验证 live ProseMirror doc 而非异步缓存被写盘。
- [ ] 保存失败 → 弹红色 sticky 错误
- [ ] 新文件首次保存 → 另存为对话框
- [ ] 有图片的文档首次保存 → 图片搬到 ./assets/,链接改相对路径
- [ ] 外部编辑器保存当前打开文件：干净标签自动刷新；本地有未保存编辑时弹出一次原生提醒，本地内容不被覆盖，提醒说明保存会覆盖外部版本且可“另存为”保留两份
- [ ] 未保存草稿 → 关闭/重启后恢复(session untitled)

## J. 导出 PDF

> 自动化和排障必须验证最终 PDF Buffer，而不是只看 source HTML。表格等视觉
> 问题的 fixture、X/Y 坐标、PNG 证据和停止条件见
> [pdf-visual-fidelity-runbook.md](./pdf-visual-fidelity-runbook.md)。

- [ ] 命令导出 PDF，或从标签/文件树右键“导出 → PDF 文档” → 打开同一个 PDF 导出中心；右侧出现真实分页预览；右键菜单靠近窗口右侧或底部时子菜单自动换向且不被裁切
- [ ] A4/A3/Letter、自定义宽高、纵向/横向、普通/窄/宽/自定义四向边距均会刷新预览
- [ ] 正文字号默认 11pt，可在 8–24pt 间按 0.5pt 调整；从 11pt 改为 14pt 后，最终 PDF Buffer 中正文文字高度应明显增长，标题、表格、代码和间距等比变化，失焦时超范围值会回落到边界、空值会恢复 11pt
- [ ] 在 8 页以上文档中，预览尚在生成时连续调整正文字号、页眉、页脚和页码；界面可持续操作，过程中不得瞬时出现 `Printing failed`，最终预览必须对应最后一次设置
- [ ] 50%–200% 整体缩放会同时缩放文字、图片、图表和页面留白；代码块和表头背景色默认保留，快速连续调整后只显示最终设置
- [ ] 分页选择“一级/二级/三级标题前” → 对应标题从新页开始；首个标题不产生空白页
- [ ] 分页选择“分隔符后” → `---` 后换页；“自动分页”不注入强制分页
- [ ] 开启“打印目录页” → 正文前增加目录；H1–H6 深度和正文另起一页开关生效
- [ ] 开启/关闭“PDF 书签大纲” → 阅读器侧栏书签随之出现/消失，不影响是否打印目录页
- [ ] 页眉标题/自定义文字/日期、页脚文字/页码在预览和导出文件中一致
- [ ] 页码范围支持 `1-5, 8, 11-13`；倒序或非法范围显示错误且不能导出
- [ ] 预览缩放和当前页计数正常；远程图片失败/超时显示警告而不是静默缺失
- [ ] 生成 PDF 后富文本样式(标题/代码/图片)正确，页面尺寸与设置一致；C++/JavaScript 等普通 fenced code block 原样输出，不会被误渲染为公式
- [ ] 段落 LaTeX 公式 `$$...$$` 导出后显示为渲染后的公式且居中，不显示源码、不显示“复制/编辑代码”等公式块控件；极长公式会按运算符分行，字号不被压缩且不被页面裁切
- [ ] 文档同时放入 Mermaid 流程图、时序图、饼图、类图、状态图和 ER 图，不滚动等待预览就立即导出 → PDF 中均为按原始比例显示的图表，不出现 Mermaid 源码；故意写错的 Mermaid 保留源码且不阻止整篇导出
- [ ] 同一 PDF 同时包含粗体/斜体/行内代码、引用、任务列表、表格、原生 HTML、LaTeX、Mermaid 和普通代码 → 结构均保留，编辑器工具条、拖拽柄、预览切换按钮不进入 PDF
- [ ] 准备一个“短 ID / 中等名称 / 长说明”三列表格，记录富文本列宽后打开 PDF Studio：紧凑表不铺满整页，三列比例与富文本一致且不等分，单元格上下留白保持富文本的紧凑密度且正文段落间距不受影响；手动拖宽第一列后重新预览，PDF 同步使用新比例。再导出 10 列以上宽表，右侧末列、长内容和表后正文均完整，不能裁切或横向溢出页面
- [ ] 无图片文档的 PDF 预览不显示“部分资源可能未完整加载”；包含空格/中文/`%20` 相对路径的本地图片和可访问的远程图片在编辑器及 PDF 中均出现且不报警；远程图片真实加载失败、超过资源限制或超时时才显示警告
- [ ] 首次预览尚在生成、导出按钮置灰时，连续切换页眉、页脚、日期和页码 → 控件立即响应，最终只显示最后配置的预览，不出现“PDF 失败”；“仍在加载”和“加载失败”提示不得混写为“0 张图片失败”
- [ ] 未打开的文件右键导出 → 自动打开挂载后再导
- [ ] 大文档首次从文件树导出 → 等待编辑器明确就绪，不因加载超过 3 秒失败
- [ ] 分别从文件菜单和文件树右键导出两个位于不同文件夹的 Markdown：首次保存对话框必须各自打开在源文件目录；为其中一个文件另选导出目录后再次导出只记住该文件，另一个文件不能串用该目录；HTML 与 Pandoc 遵循同一语义
- [ ] 快速连续调整多个选项 → 旧生成任务被取消，最终预览与最后一组设置一致且无残留隐藏窗口
- [ ] 生成中关闭导出中心/关闭窗口 → 任务与临时文件被清理，不继续占用 CPU
- [ ] 排版密度选「紧凑」→ 预览页数明显减少（实测约 -19%）；选「舒适」页数增多；「标准」与升级前完全一致。密度下方实时显示「约 N 页」，生成中显示「估算页数中…」
- [ ] 排版密度只影响间距：字号、页边距、整体缩放仍独立可用；紧凑 + 窄边距可进一步减少页数
- [ ] 选过一次「紧凑」后关闭再打开 PDF Studio → 仍记住紧凑（只记密度，不记本次的页眉/页脚/标题/页码范围）
- [ ] 导出 PDF 保存对话框默认在当前 Markdown 所在目录；把 A 文件另存到别处后，再导出 A 仍默认那个目录；导出另一个文件夹的 B 文件仍默认 B 自己的目录

### J.1 HTML 与 Pandoc 导出

- [ ] 标签和文件树右键均只显示一个“导出”一级项，二级菜单同时提供 PDF、HTML 和六种 Pandoc 格式；两处格式、顺序与禁用能力一致，选择非当前文件会先正确挂载该文件再导出
- [ ] 文件菜单或命令面板打开“导出 HTML” → 左侧设置、右侧预览和底部导出按钮完整显示，Escape 可关闭
- [ ] 依次切换简洁/纸张/阅读/夜间主题与紧凑/标准/宽幅/全宽 → 预览即时变化且不出现横向撑开；字号、行高滑杆的可点击位置与视觉轨道一致
- [ ] 开关文档标题和页面目录，并将目录层级从 H1 改到 H3 → 预览只收录对应标题且条目可点击；最终保存文件与最后一次预览完全一致
- [ ] 含任务列表、自然列宽表格、普通代码、LaTeX、Mermaid、本地图片和远程图片的文档 → HTML 显示结构化结果，不出现编辑器控件或 Mermaid/公式源码；断网时无法嵌入的远程图片保留地址而不阻塞其余文档
- [ ] 用浏览器查看导出 HTML 源码 → 不含 `script`、`iframe`、`object`、`embed`、`form`、`on*` 事件或 `javascript:` URL；普通外部链接与图片仍可用
- [ ] 未安装 Pandoc → 设置“文件与图片 → 文档转换工具”明确显示未检测到；导出时给出安装入口，不创建空文件
- [ ] 安装 Pandoc 后重新检测，或手动选择 `pandoc`/`pandoc.exe` → 显示版本与实际路径；选择其他程序必须拒绝
- [ ] 分别导出 Word、EPUB、LaTeX、ODT、RTF、TXT → 使用当前未保存编辑内容；相对图片按 Markdown 所在目录解析；成功后文件管理器定位结果
- [ ] Pandoc 语法失败、图片缺失、两分钟超时或用户取消保存 → 状态明确、应用不挂死、原 Markdown 不变且不标脏
- [ ] HTML 与 Pandoc 导出的保存对话框同样默认在源 Markdown 所在目录，并按文件记住改过的目录（与 PDF 一致）
- [ ] 移动端不显示 HTML/Pandoc 入口；`npm run build:mobile` 仍通过

## K. 富文本基础(打字即渲染)
- [ ] 标题 H1–H6、有序/无序/任务列表、引用、链接、分隔线、粗/斜/删除线
- [ ] 标题大小写：H5/H6 输入 `MixedCase` / `camelCase` 后，富文本必须按原样显示，不能强制转成全大写(#63)
- [ ] 表格:增删行列、单元格 Enter 换行(往返 `<br>`,#7)、源码往返不坏；连续新增两行和两列后填写最后一行的全部单元格，切源码/保存后每项仍在原列，未填写的单元格为 `| |` 而非 `<br />`，表格不额外长出空行或空列；关闭文件并重新打开后再次确认以上结果。宽表滚到最右后右键列手柄/列菜单，横向位置不回到左端(#86)
- [ ] 表格视觉与宽度:新建包含“ID / 简短名称 / 较长说明”的表格，三列必须按所有单元格内容形成不同宽度，不能默认等分；继续修改较长说明时，尚未手动调宽的列会自适应。短表保持内容所需宽度、不拉满编辑区，表体有细微底色且与表头可分辨；在 12px、16px、24px 文档字号下，单元格内边距和行高随字体等比变化，小字号不保留固定高行；切换浅色/深色主题后仍成立。普通悬停列边界只出现加列按钮；按住约 220ms 才进入列宽调整，拖动过程中实际列宽实时变化，1px 黄色引导线完整覆盖表格高度；松手后保存、重开仍优先使用手动列宽；加行/加列后仍可继续拖动列宽
- [ ] 宽表格:多列/长文本保持可读列宽,表格内部可左右滑动且滚动条紧贴最后一行;编辑区和整个应用不被横向撑开。设置 → 外观 → 表格 → “宽表自动换行”下方的预览关闭时展示横向溢出、开启后展示变高的折行单元格；Markdown 表格随该开关切换滚动/换行。原生 HTML `<table>`（含固定像素宽度的嵌套表，如 gov.cn 的 `<table width="950">`）默认就跟随正文宽度收缩、列折行换行，不横向溢出；把排版编辑区拖窄到约一半宽度，HTML 表格仍完整显示不裁切。在窄编辑区和移动宽度分别检查。
- [ ] 高且宽的表格:分别在表格顶部/中部/底部、横向滚动条左端/右端悬停并选中行列;行列手柄及操作菜单不被标题栏、编辑区或表格分块遮挡,菜单内每个按钮均可点击。选中后故意将鼠标移向菜单，菜单应至少短暂停留；鼠标进入菜单后不自动消失，移出后才自动收起
- [ ] 表格单元格边界:悬停后“加行/加列”按钮完整可见且可点击;分别点击后实际增加一行/一列
- [ ] 行内代码闭合触发：真实逐键输入左反引号，再用真实中文 IME 输入“中文”；此时必须仍显示字面 `` `中文 ``，不得提前渲染、不得出现临时边界。输入最后一个右反引号后才渲染为行内代码，切源码严格为 `` `中文` ``
- [ ] 行内代码闭合后退出：完成 `` `中文` `` 后直接继续输入 `outside`，新文字必须是普通正文，不得无限继承 code（#10）
- [ ] 行内代码方向键退出：点击已渲染代码的尾部按 `→`，再输入 `outside`；点击首部按 `←`，再输入 `before`。新文字都必须在代码外；连续方向键应继续进入相邻正文，不能卡在 mark 的同一文档位置，也不能插入占位字符
- [ ] 行内代码新段落：在已有末段后按 Enter，逐键输入完整 `` `feaef` `` 后输入 `212afea`；富文本应为独立段落，源码必须包含 ``上一段\n\n`feaef`212afea``，不能与上一段合并。连续执行 10 次
- [ ] 反引号运行：单个未闭合反引号、两个反引号、三个反引号都不得吞字符；``` 后按 Space 可进入标准代码块，空代码块按一次 Backspace 退出后立即快速输入正文，源码/保存/重开不得与上一段合并。另在真正空白文件中逐键输入 ```` ```你好``` ````（真实中文 IME），富文本保持普通正文，源码不得变成带 `\` 的 canonical 拼写，保存重开仍逐字一致。自动化：`npm run test:inline-code-ui`、`npm run test:code-fence-delete-source-ui`、`npm run test:literal-triple-backtick-source-ui`
- [ ] 已有 `` `ab` `` 点击其右边缘并输入 `c` → 变成 `` `abc` ``；点击代码外正文仍输入普通文本(#58)
- [ ] 斜杠 `/` → 块菜单(中文);块拖拽 handle 可用

## L. 代码块(CodeMirror)
- [ ] ``` 代码块 → 语法高亮 + 深色代码面 + Copy 按钮
- [ ] 点击代码块 Copy → 系统剪贴板得到完整代码（含换行/空行），只有写入成功才显示“已复制”；用超过 120 行的 `settings.json` 验证，即使 CodeMirror DOM 只渲染约 30–65 行，也必须从第一行复制到最后一个 `}`，不能按可视区域截断
- [ ] 同一长代码块内分别执行 `Ctrl/Cmd+A` 全选复制，以及从第一行 Shift 选择到第 65 行再复制 → 全选必须得到完整代码，部分选择必须精确止于选区末尾；不能复用上一次剪贴板内容，也不能因虚拟滚动多复制或少复制
- [ ] 在富文本中只选中一个正文段落并复制到记事本/纯文本输入框 → 内容前后不增加回车或空行；选中有序列表项的文字 → 不额外出现 `1. `
- [ ] 表格单元格内**单击**即可直接放置光标并输入（无需双击）；在单元格内按住鼠标拖选文字能正常选中并复制，不会误选整格/整表
- [ ] 代码块左侧行号：右对齐、颜色柔和、与代码行高对齐、带细分隔线；复制代码时不包含行号；导出 PDF 后代码块同样带行号且代码内容逐字一致
- [ ] 复制含普通源码单换行的多行正文到纯文本目标 → 每条视觉行之间恰好一个换行，不合并成一行、不变成空行；粘到富文本目标时 HTML 使用 `<br>` 保持相同行数
- [ ] 在富文本中复制加粗、行内代码或完整列表，再粘回 HorseMD → 仍恢复对应格式和列表结构；粘到支持富文本的外部应用 → 使用 HTML 保留可读样式
- [ ] 富文本输入一段文字后按 `Ctrl/Cmd+Z` → 本次输入撤销；菜单“编辑 → 撤销/重做”仍可用
- [ ] 富文本代码块前后间距保持紧凑并随“段落间距”同步变化，语言选择和 Copy 按钮仍可用(#80)
- [ ] 代码块可编辑;Mermaid/LaTeX 块走预览机制(见 L1/M)
- [ ] **#25 跳页**:滚到代码块、停留、**选中文字** → 内容**不跳**。CodeMirror 必须 eager-mount 且不 teardown；大文档还必须让 `.milkdown-code-block` 退出 `content-visibility` 的 5em 高度估算。执行 `npm run test:codeblock-scroll-stability-ui` 覆盖 `.hm-cv` 下四处代码块。

## L1. Mermaid
- [ ] ```mermaid → 默认渲染图,工具栏 Hide/Edit 切换源码
- [ ] 在正文粘贴裸 Mermaid 或完整 ` ```mermaid ` 围栏 → 每次只创建一个代码块和一个 SVG；切源码、保存重开后仍只有一份
- [ ] Mermaid 节点标签包含 `flowchart TD`、`sequenceDiagram` 等声明文字 → 仍是一张图，不得按标签误拆成两块
- [ ] 在已有非空 Mermaid 源码块中粘贴第二段 Mermaid → 精确新增一个同级块；两段源码和两个预览分别保留，不拼接、不重复
- [ ] 点击 Mermaid 的“编辑代码”，逐字输入一条新连线（可先故意停在 `B -->`，再补 `C`）→ 预览最终必须刷新为最新图，不得被中间语法错误覆盖；立刻保存、切源码并关闭重开后，新连线和新图都必须保留
- [ ] 主题切换 → mermaid 重渲染配色

## M. LaTeX 数学(#18 + 渲染修复 + 打字)
- [ ] `$$x^2+y^2$$` 单行 → 渲染成 **KaTeX 块公式**(不是裸代码块)
- [ ] `$$\n…\n$$` 多行 → 同样块公式
- [ ] 带编号的块公式，例如 `$$MRS_R = MU_R/MU_I\tag{13-3}$$` → 公式主体居中、`(13-3)` 位于右侧预留编号栏，两者不重叠；窄编辑区仍在公式块内部横向滚动，不撑开正文
- [ ] 正常宽度的块公式不显示滚动条或滚动箭头；只有超宽公式才在公式自身内部横向滑动，外层 LaTeX 预览不出现第二组滚动控件
- [ ] 块公式与上下正文的留白明显小于普通代码块，且切换为公式源码编辑态后仍有足够的控件与输入留白；普通代码块的间距不变化
- [ ] 内联 `$E=mc^2$` → 内联 KaTeX
- [ ] **打字** `$$x^2$$`(单行,直接在段落里打)→ 变成块公式(不是行内)
- [ ] **打字** `$E=mc^2$` → 行内公式(单 `$` 不受块规则影响)
- [ ] 先输入 `abc$$`,把光标移到两个 `$` 中间连续输入 `1^2` → 输入不中断、KaTeX 实时预览;光标离开后渲染为 `$1^2$`(#68)
- [ ] 先输入 `$$`,在中间输入纯数字 `123` → 同样实时预览并在离开后渲染(#68)
- [ ] 点击已有行内公式并修改源码 → 编辑框下方预览逐字更新;确认后正文公式与新源码一致(#69)
- [ ] 富文本↔源码往返 → 公式不坏
- [ ] Mermaid 和 LaTeX 同时存在 → 两者都渲染(renderPreview 链式未回退)
- [ ] 输入 `$$` 后回车，连续输入 `x^2 + \sqrt{4}` → CodeMirror 不隐藏、光标不中断、预览同步(#57)
- [ ] `/math` 回车 → 当前段落直接变成 LaTeX 块，光标位于公式源码内而不是下一行(#57)

## N. 高亮 / 行内 HTML(#14)
- [ ] `==text==` → 黄色高亮;工具栏高亮按钮可选红/黄/蓝;⌥⌘H 切黄
- [ ] 行内 HTML `<span style=color:red>x</span>` → 渲染;块 `<table>` HTML → 渲染表格
- [ ] 行内 HTML `<font color=#F36208>文字</font>`、`<span style="background:#affad1">文字</span>` 内悬浮 → 不出现块拖拽柄；移到编辑区左侧块操作热区 → 拖拽柄仍可出现并可用
- [ ] 源码往返保留原始 HTML

## O. Front matter
- [ ] 文档顶部 `---` yaml → 渲染成 key/value 卡片;复杂 yaml → 代码框;点击“编辑 YAML”可在富文本内修改，完成后卡片和源码都更新且相邻正文不变
- [ ] 正文中的 `---` → 保持 Markdown 分隔线；其后的 `### Q3:`、`### Q4:` 等标题不应被解析为 YAML 卡片

## P. 粘贴
- [ ] 粘贴 markdown(`#`/表格/引用/```/$$)→ 解析成富文本(智能粘贴)
- [ ] 从微信公众号复制多个 `section`/`div` 段落 → 段落保持分开，加粗/斜体/列表不丢
- [ ] 复制包含编号小标题和懒加载图片的微信公众号长文 → 优先保留 HTML 标题层级、段落、加粗和图片，不误判成 Markdown 列表
- [ ] 粘贴 mermaid 源码 → mermaid 块
- [ ] 粘贴图片 → 写入 ./assets/(已保存文档)或全局粘贴目录(草稿),显示
- [ ] 粘贴进代码块 → 追加代码,不重排
- [ ] 短文档中分别点击正文下方、页面容器下方、左右侧和窗口底部留白并输入 → 光标均落在文档最后；点击滚动条与交互控件不被拦截

## Q. 布局 / StatusBar
- [ ] StatusBar:字号 / 行距 / 段距 / 标题间距 / 页宽 滑块 → 实时应用;拖动顺滑(直写 CSS var)
- [ ] 块类型切换(段落/H1…);源码切换;图床按钮
- [ ] 统计:字数/字符/行数 实时更新

## R. 图床
- [ ] 配置图床命令 → 选中图片上传 → 返回 URL 插入
- [ ] 空命令 → 不拦截粘贴(无死 blob:)

## S. review 批注/修订(PR #21 + 卡片 UI 改造)
- [ ] 工具栏「审阅标记」→ 新增/删除/替换/高亮+评论 四种
- [ ] `{++add++}`/`{--del--}`/`{~~old~>new~~}`/`{==hl==}{>>c<<}` 富文本渲染
- [ ] **序号按钮**:每个高亮评论右侧有序号(全局递增 1,2,3…)
- [ ] **同行多个高亮 → 右侧叠放(stack)**:多个序号收到右侧一个 stack,收叠时重叠 ~57%;**悬浮展开**(JS class 驱动,不是 CSS :hover → 不抖动);展开后**完全分离、无重叠**
- [ ] **点击序号** → 打开该条备注卡片(卡片头显示 X/Y,如「2 / 5」)
- [ ] **卡片内容**:高亮文本(小、淡)+ 评论(accent 色块,视觉主体)
- [ ] **卡片按钮**:图标按钮(✏️编辑/✓完成/✕删除/📋复制标记/✨复制AI/📄复制段落)+ tooltip
- [ ] **卡片导航 ‹ ›**:多条备注间切换(切 openGroupKey + 滚动带入视野)
- [ ] **悬浮展开不抖动**:鼠标在展开的序号间移动,稳定不闪烁(JS class + transform)
- [ ] **大文档(≥20k)**:单个高亮序号**实际可见**(不被 `.hm-cv` 的 contain:paint 裁掉)
- [ ] **空评论高亮**仍渲染序号(工具栏插入空评论 → 立刻有序号)
- [ ] 「全部接受/拒绝」→ 各标记正确处理
- [ ] 源码↔富文本往返 → 标记不丢/不坏
- [ ] **中文输入法**(Mac)打替换标记 → 不被 compositionend 破坏(e530fe6)
- [ ] ⚠️ 已知:review 标记在**源码模式**失效(非受控 textarea + updateContent)—— 测时只测富文本模式

## T. 性能(大文档)
- [ ] 打开 ~5万行文档 → 快速打开(textarea 回退);点横幅切富文本 → 分块渐进渲染,不冻
- [ ] 富文本大文档:滚动顺(content-visibility)、打字不卡、大纲跟手
- [ ] >1000 行无空行的文件 → 仍 textarea(安全网);普通长代码块(≤1000 行)→ 富文本(阈值已调)
- [ ] 快速滚动后松手 → 不再追赶(#17,Windows 重点)

## U. 移动端(分平台,Capacitor)
- [ ] iOS/Android:抽屉侧栏、状态栏不重叠(小米/MIUI)
- [ ] 在文档顶部/底部继续上下拖动:只滚动编辑区,整个应用外壳不位移,底部不露空隙
- [ ] 宽 Markdown/HTML 表格可单指左右滑动,横向滑动不带动整个页面
- [ ] 双击/长按选择富文本 → 只显示系统原生菜单，不出现 HorseMD 选区工具栏；复制、粘贴、全选正常
- [ ] 保存到本地文件库 → 弹「已保存 ✓ + 位置」sticky 提示
- [ ] 编辑/查找/主题/大纲 基本可用

## V. 设置页(ActivityBar 齿轮 / 移动端 •••)
- [ ] 左下角齿轮按钮 → 开设置 tab;设置 tab 激活时齿轮高亮;再点齿轮幂等(不开第二个)
- [ ] 设置 tab:EditorArea 隐藏、`<SettingsView>` 显示;切回文档 tab → 编辑器重新可见(不重建)
- [ ] 关设置 tab → 回到原文档 tab
- [ ] **不持久化**:设置 tab 开着关窗口/重启,不恢复(CDP 测过 session LS 无 settings tab)
- [ ] **分类与顺序**：“编辑器”只显示校对和编辑行为，不出现字体、排版预览、自定义 CSS 或表格预览；“外观”依次显示主题、排版预览、自定义 CSS、表格和源码外观。自定义 CSS 必须紧跟排版预览并位于表格之前
- [ ] **排版**:字号/行距/段距/标题间距/页宽滑块拖动 → 下方实时预览 + 切回文档正文同步变；页宽在“窄 / 中 / 宽 / 全宽”之间必须逐级变宽，两侧留白同步缩小，按住微调滑杆尚未松手时预览就应变化；标题间距只改变 H1–H6，不连带改变正文、列表或代码块
- [ ] **启动恢复（#98）**:通用 → 关闭“恢复上次打开的文档” → 重启不恢复历史文件和未保存草稿；随后从 Finder/资源管理器或命令行明确打开 Markdown → 文件仍正常出现。重新开启后，下一次启动恢复当前会话
- [ ] **列表排版（#79）**:设置行距/段距为紧凑、宽松各一次 → 无序列表、有序列表和嵌套列表的行高、项间距与前后留白同步变化；标记与正文首行垂直居中，首层和每一级子列表均为紧凑、稳定的缩进；真实编辑器和设置预览的标记颜色一致且不应退化为低对比度边框色
- [ ] **列表类型转换（桌面富文本）**：分别右键父列表和子列表 → “转换为有序/无序列表”或“转换为待办清单”；每次仅当前层级改变，父层与子层类型不变，文本和缩进保留。任务列表可转为普通列表且移除勾选状态；列表菜单不显示正文/标题项。转换父层和子层后，光标都必须留在原项目，不能跳到最后一项。另用“外层项目间有空行、内层项目间无空行”的混合嵌套列表，转换外层后立即逐字输入，不等待界面稳定就切源码并保存；内层缩进/空行必须逐字节不变，退出应用并重新打开后富文本与源码仍一致。连续执行两次反向转换后内容和结构仍正确。
- [ ] **新文档嵌套列表退出（桌面富文本）**：新建空文档，逐字手打标题、正文、`1. 第一项`、`2. 第二项`、Tab 后的 `1. 嵌套项`；在嵌套项末尾连续按 Enter 三次，逐字输入 `- 无序项` 后立即切源码。源码必须保留标题、正文、有序层级和 `- 无序项`，不得出现空的 `3.`、默认 `*`、`<br />` 或合并行；保存、完全退出并重开后逐字一致。
- [ ] **RS-45 新文档空有序项 Tab 缩进**：真正空文件默认 H1 逐字输入 `你好`，正文逐键创建 `1. 测试`，按 Enter 得到空 `2.`，不要输入正文，直接按 Tab。富文本应变为第一项下方的空嵌套 `1.`，不得弹“源码与富文本不一致/保存已暂停”。立即切源码必须得到不含 `<br />` 的 `1. 测试\n   1. `；再往返、保存并完整新进程重开，嵌套结构和磁盘源码保持一致。专项见 `docs/generated-scratch-empty-ordered-indent-regression.md`。
- [ ] **新文档回访首个无序项（桌面富文本）**：承接上一项，Enter 新增第二个 `-` 项，再回到第一项逐字追加一个字符；不等待回调立即切源码。两项都必须保持 `-`，不能出现首项 `*`、次项 `-` 的混用；保存、完全退出并重开后仍逐字一致。自动化：`npm run test:new-document-list-source-ui`。
- [ ] **列表删除后再创建（桌面富文本）**：承接上一项，逐字删除无序项文字，在空项按 Enter 离开列表；继续逐字输入 `1. 重新有序项`，再 Enter、Tab 输入嵌套项后立即切源码。新项必须保留用户输入的 `1.` 而非 `1)`，已有 `1.`、`2.` 和嵌套层级不得被改号、合并或错位；保存、完全退出并重开后逐字一致。
- [ ] **任务清单保存**：准备一个未勾选项和一个已勾选项；点击未勾选项、保存、完全关闭并重新打开，状态仍为已勾选且源码只把该行 `[ ]` 改为 `[x]`。再取消勾选、保存并重开，状态恢复为未勾选；另一个任务项和其余源码字节不变。
- [ ] **正文转列表（桌面富文本）**：在正文段落 A 上右键，在“转换为”中分别检查无序、有序和待办清单；点击任一项只转换 A，不能误改此前光标所在的正文 B。待办项初始未勾选；切源码确认语法为 `-`、`1.` 或 `- [ ]`。
- [ ] **块操作条（桌面富文本）**：在窄、宽和全宽编辑区分别把鼠标移到标题/普通正文左侧真实留白，以及无序列表圆点 SVG、编号、待办框 → “新增段落”加号和拖拽柄完整位于正文左侧同一轨道；鼠标从留白/标记移到加号或拖拽柄时操作条不消失。重点把鼠标放在普通段落第一个字符及其后约 36px 范围，操作条绝不能出现；悬浮列表文字、彩色/高亮内联文本也绝不出现。拖动侧栏或调整窗口尺寸后再次检查，两个按钮仍完整且不遮挡文字。
- [ ] **macOS 无窗口重开**：关闭最后一个 HorseMD 窗口但不退出应用，再从 Dock 点击 HorseMD 或双击任意 Markdown 文件 → 必须重新出现窗口；后者还必须打开该文件，不能停留在空白或无窗口状态。
- [ ] **自定义 CSS 预览与停留**：预览中可见标题、粗体/斜体/删除线、链接、行内代码、`kbd`、引用、普通/有序/任务列表、表格和代码块；建立两个 CSS 片段后编辑第二个并使其内容提交，切到任意文档再回设置，仍选中第二个片段且已提交 CSS 内容仍显示并生效
- [ ] **校对**:拼写开关 关→无红线、开→英文红线;切 tab 一致;重启保持设定值
- [ ] **外观**:主题色块切换生效(亮/暗/莫兰迪/自定义);「打开主题目录」「获取更多主题」可用
- [ ] **语言**:中/英切换,设置页 + 全 UI 文本跟随
- [ ] **图床**:输入命令 → 保存;清空 → 粘贴图片不拦截
- [ ] **键盘快捷键**:可搜索、录制、清空、单项恢复、全部恢复;冲突和保留键提示清晰;固定编辑器命令显示为不可修改
- [ ] **关于**:版本号显示;GitHub/Gitee 链接可点
- [ ] 设置页时:保存 FAB 隐藏、⌘S/导出 不作用于设置页
- [ ] **StatusBar 快捷控件不回归**:排版/主题/语言 仍正常(LayoutControl 抽 AdjustGroup 后)
- [ ] 移动端:••• sheet 有「设置」入口;设置页布局正常(无 activity bar)

## W. Mermaid 全屏灯箱(v0.5.0)
- [ ] 渲染好的 mermaid 流程图 → 点击 → 全屏灯箱弹出(含克隆 SVG)
- [ ] 横向/纵向长图 → 灯箱严格保持原始比例，容器没有固定方形造成的大块空白
- [ ] 缩小/放大按钮与倍率显示同步；适应窗口回到 100%；1:1 恢复原始像素尺寸
- [ ] 灯箱内 Ctrl+滚轮 → 缩放(0.2×–10×);按住拖拽 → 平移
- [ ] Esc / 点背景 → 关闭;拖拽后的 click 不误关
- [ ] 图片灯箱(双击图片)仍正常，小图不被强制放大，长图宽高比不变

## X. 标签拖拽 + 隐藏文件 + 拼写检查(v0.5.0)
- [ ] 拖标签到新位置 → 顺序变 + 重启保持;关闭/右键/中键不回归
- [ ] 设置 → 外观 →「显示隐藏文件」开关 → .claude 等出现;.git/node_modules 仍隐藏
- [ ] 设置 → 校对 → 拼写检查开关 → 开有红线、关无;跨 tab 一致 + 持久化

## Y. 大纲跳转 + 模式切换位置(v0.5.0)
- [ ] 小文档多标题:点最后一条大纲 → ease-out 动画 + 精准落点 + 高亮 = 点击的那条
- [ ] 大文档(≥20k):加载中点大纲 → 加载完再跳(不显示部分标题);落点准
- [ ] 富文本停在某标题 → ⌘/ 切源码 → 仍在该标题附近 → 切回 → 仍准
- [ ] 纯滚动到代码块 + 停下 + 选中文字 → 不跳(eager-mount #25)

## Z. Windows 快捷键(v0.5.0,Windows 实机)
- [ ] 选中文字按 Ctrl+B → 加粗/取消加粗，不切换侧边栏；Ctrl+Shift+B → 只切换侧边栏(#67)
- [ ] Ctrl+W → 只关标签页(不关整个应用);关最后一个标签 → 回 Home 不退出
- [ ] Alt+F4 → 关闭窗口(菜单里 Close 绑定 Alt+F4)
- [ ] macOS:Shift+Cmd+W 关窗口(不变)

## ZL. Linux 安装与桌面集成（Ubuntu/Debian x64 实机）

- [ ] `dpkg-deb --info dist/horse_*_amd64.deb` 成功，包不是 macOS 交叉构建产生的无效小文件
- [ ] `sudo apt install ./horse_*_amd64.deb` 自动补齐依赖并完成安装
- [ ] 应用菜单显示 HorseMD 和正确图标；点击后正常启动且只出现一个实例
- [ ] 文件管理器双击 `.md` / “打开方式 → HorseMD”能打开对应文档
- [ ] 顶栏可拖动；最小化、最大化、还原、关闭及图标状态同步正常
- [ ] 工作区、文件 watcher、Mermaid、图片、源码/富文本切换和 PDF 导出各做一次冒烟测试
- [ ] 终端 `/opt/HorseMD/horse` 可启动；文档不声称存在未安装的 `horsemd` 全局命令
- [ ] `sudo apt remove horse` 可卸载，用户自己的 Markdown 文件不受影响



## AA. 字体设置（#38）

- [ ] 设置 → 外观 → 排版 → 文档字体：点击 → 弹出搜索+列表，完整字体名清晰可读，过长名称有原生提示
- [ ] 悬停字体项 → 右侧预览 + 编辑器正文实时变（不点也变）
- [ ] 点击选中 → 字段显示该字体名（深色，不灰），编辑器生效
- [ ] 选默认 → 清空字体，回默认栈
- [ ] 代码字体同理（代码块字体跟着变，包括 CodeMirror 的 .cm-content）
- [ ] 重启 app → 字体设置保持
- [ ] 列表底部浏览更多字体→ 方正字库（foundertype.com）
- [ ] 代码字体列表底部 → Nerd Fonts（nerdfonts.com）
- [ ] 文档字体和代码字体的搜索框：打开后立即输入，查询不会被异步加载重置，焦点保持在输入框且列表会过滤
- [ ] 设置预览区：文档字体变化 → 预览正文跟着变（不是界面字体）

## AB. 大纲折叠 / 展开

- [ ] 大纲顶部只有一个展开/折叠总开关;图标随全折叠状态切换
- [ ] 当前阅读子标题时,点击父标题箭头 → 父级可折叠,不会无反馈
- [ ] 子标题被折叠后,父标题显示 active/contained 提示;再次展开后子标题恢复显示
- [ ] 源码模式大纲仍可显示、点击跳转、滚动高亮

## AC. 插入附件（#49）

- [ ] 未保存文档执行「文件 → 插入附件…」或命令面板 → 提示先保存
- [ ] 已保存文档选择 PDF/DOCX/ZIP → 同级 `assets/` 出现副本,光标处插入 `[name](<assets/name.ext>)`
- [ ] 源码模式插入附件 → 链接插到 textarea 光标处,切回富文本后仍是链接
- [ ] 富文本模式插入附件 → 链接插到当前光标处,保存后 Markdown 正确
- [ ] 重名附件 → 自动生成 `name-1.ext`,不覆盖旧文件

## AD. Slash 命令菜单边界

- [ ] 顶部段落新起一行输入 `/` → 菜单完整可见,不被顶部栏遮挡
- [ ] 底部段落新起一行输入 `/` → 菜单完整可见,不被底部状态栏遮挡
- [ ] 小窗口/窄窗口底部输入 `/` → 菜单高度收缩,内部可滚动

---

## 重构回归优先级
- **每次必跑**:A(标签)、D(源码/富文本)、H(查找替换,尤其源码模式)、I(保存)、M(LaTeX)、S(review 富文本)、V(设置页)。
- **性能项(T)**:动到 Editor.jsx / scrollspy / 非受控 textarea 后必跑。
- **全量回归**:每个 hook/模块抽完后,整表过一遍。
- **性能项(T)**:动到 Editor.jsx / scrollspy / 非受控 textarea 后必跑。
- **全量回归**:每个 hook/模块抽完后,整表过一遍。
